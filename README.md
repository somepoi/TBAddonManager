# Tiny Bunny Addon Manager

> Менеджер аддонов для визуальной новеллы [Tiny Bunny](https://saikono.com/)

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.11-blue.svg)
![RenPy](https://img.shields.io/badge/RenPy-8-red.svg)

## 📖 Описание

TBAddonManager — это легкий и удобный лаунчер для визуальной новеллы Tiny Bunny, созданный на базе RenPy 8. Он позволяет разработчикам аддонов и модификаций легко настраивать параметры запуска игры, включая режим разработчика, консольные инструменты и другие опции для отладки.

## Функции

- **Удобный запуск игры** — запускайте Tiny Bunny с нужными параметрами в один клик
- **Режим разработчика** — включение `config.developer` для доступа к функциям разработки
- **Внутриигровая консоль** — активация `config.console` для выполнения команд RenPy
- **Внешняя консоль** — запуск игры с отдельным окном консоли для удобной отладки
- **Многоязычность** — поддержка русского и английского языков с автоматическим определением локали
- **Сохранение настроек** — автоматическое сохранение последних выбранных опций
- **Современный интерфейс** — темная тема с использованием Qt6

## 📋 Требования

- **Python**: 3.11.x
- **RenPy**: 8.x
- **Операционная система**: Windows (тестировалось на Windows 10/11)
- **Игра**: Tiny Bunny должна быть установлена

### Зависимости Python

```toml
PySide6 >= 6.10.1
pyinstaller >= 6.17.0
```

## Установка

### Для пользователей

1. Скачайте последний релиз из раздела [Releases](../../releases)
2. Распакуйте файл `launcher.exe` в корневую папку игры Tiny Bunny
3. Запустите `launcher.exe`

### Для разработчиков

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yourusername/TBAddonManager.git
cd TBAddonManager
```

2. Установите PDM (если ещё не установлен):
```bash
pip install pdm
```

3. Установите зависимости:
```bash
pdm install
```

4. Запустите программу:
```bash
pdm run run-main
```

## Использование

1. Запустите лаунчер из корневой папки игры Tiny Bunny
2. Выберите нужные опции:
   - **Режим разработчика** — для доступа к функциям RenPy для разработчиков
   - **Внутриигровая консоль** — для выполнения команд прямо в игре (Shift+O)
   - **Внешняя консоль** — для просмотра логов и ошибок в отдельном окне
3. Нажмите "Запустить игру!"
4. Игра запустится с выбранными параметрами

### Настройки сохраняются автоматически

Все выбранные опции сохраняются в файл `game/TBAddonManager.ini` и автоматически восстанавливаются при следующем запуске.

## Разработка

### Структура проекта

```
TBAddonManager/
├── main.py                      # Точка входа приложения
├── main.spec                    # Конфигурация PyInstaller
├── pyproject.toml              # Конфигурация проекта и зависимости
├── icon.ico                     # Иконка приложения
├── internal/                    # Основной код
│   ├── __init__.py
│   ├── interface.py            # GUI интерфейс (PySide6)
│   ├── config.py               # Работа с конфигурацией
│   ├── lang.py                 # Система локализации
│   ├── resources_rc.py         # Скомпилированные ресурсы Qt
│   └── resources/              # Ресурсы приложения
│       ├── resources.qrc       # Файл ресурсов Qt
│       ├── icon.ico            # Иконка
│       ├── face.png            # Изображение
│       ├── title.ttf           # Шрифт для заголовков (Razor Keen)
│       └── body.ttf            # Шрифт для текста (Kazmann Sans)
├── build/                       # Временные файлы сборки
└── dist/                        # Собранное приложение
```

### Доступные команды PDM

```bash
# Запуск приложения
pdm run run-main

# Компиляция ресурсов Qt
pdm run run-qrc

# Сборка исполняемого файла
pdm run run-build
```

### Сборка исполняемого файла

1. Убедитесь, что все ресурсы актуальны:
```bash
pdm run run-qrc
```

2. Соберите приложение:
```bash
pdm run run-build
```

3. Исполняемый файл появится в папке `dist/launcher.exe`

### Добавление новых переводов

Откройте файл `internal/lang.py` и добавьте новые строки в словарь `lang`:

```python
lang = {
    "Your String": {
        "ru_RU": "Ваша строка",
        "en_US": "Your String"  # опционально, если отличается от ключа
    }
}
```

Используйте в коде:
```python
from internal.lang import gt

text = gt("Your String")  # Автоматически выберет язык системы
```

## Технические детали

### Интеграция с Tiny Bunny

Лаунчер проверяет наличие следующих компонентов:
- `lib/py3-windows-x86_64/python.exe` — интерпретатор Python от RenPy
- `TinyBunny.py` — основной скрипт игры

Конфигурационный файл создаётся по пути `game/TBAddonManager.ini` и содержит:
```ini
[gameconfig]
enabled = 1
developer = 0/1
console = 0/1
extconsole = 0/1
```

### Используемые технологии

- **PySide6** — Qt6 bindings для Python (GUI)
- **PyInstaller** — упаковка в исполняемый файл
- **PDM** — управление зависимостями
- **Qt Resources** — встраивание шрифтов и изображений

## Вклад в проект

Приветствуются любые предложения по улучшению проекта! 

1. Создайте Fork репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## Лицензия

Распространяется под лицензией MIT. См. файл `LICENSE` для дополнительной информации.

## Авторы

- **ohfkgsofsex** - *Основной разработчик* - [abuanin5@gmail.com](mailto:abuanin5@gmail.com)
- **tecquo** - *Со-разработчик*

## Благодарности

- [Saikono](https://saikono.com/) — разработчики визуальной новеллы Tiny Bunny
- RenPy — игровой движок
- Сообщество модостроения Tiny Bunny

---

**Примечание**: Это независимый проект, не связанный официально с разработчиками Tiny Bunny.