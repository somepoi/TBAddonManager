# TBAddonManager

**Менеджер аддонов для визуальной новеллы "Зайчик" (Tiny Bunny)**

## Описание

TBAddonManager — это менеджер модификаций для визуальной новеллы Tiny Bunny. Включает в себя удобный графический интерфейс для просмотра, поиска и запуска установленных аддонов прямо из главного меню игры.

### Основные возможности

Вот этот раздел бы поправить, текстом описать. Убрать лишние, интуитивные штуки.
- **Интеграция с главным меню** — кнопка запуска менеджера прямо в главном меню игры
- **Автоматическое обнаружение аддонов** — парсинг `addon.ini` файлов из всех папок игры
- **Умный поиск** — поиск по названию, описанию и авторам с возможностью сортировки по алфавиту
- **Визуальное представление** — отображение аватаров, названий, описаний и авторов
- **Мультиязычность** — встроенная поддержка 6 языков
- **Нативный дизайн** — интерфейс в стиле оригинальной игры
- **Режим разработчика** — опциональная консоль для отладки

## 📦 Установка

1. Скопируйте папку `TBAddonManager` в директорию `game/` новеллы:
   ```
   D:\GAMES\TinyBunny\game\TBAddonManager\
   ```

2. Запустите launcher.exe

3. Выберите необходимые настройки и запустите игру

(не забыть напомнить о том, что не нужно каждый раз запускать лаунчер, можно и без него запускать и всё будет работать)

## Создание аддонов

### Структура аддона

Каждый аддон должен содержать файл `addon.ini` в своей корневой папке:

```
game/
└── YourAddon/
    ├── addon.ini
    ├── script.rpy
    └── images/
        └── avatar.png
```

### Формат addon.ini

```ini
[your_addon_label]
name = Название вашего аддона
description = Подробное описание\nс поддержкой\nмногострочного текста
avatar = YourAddon/images/avatar.png
authors = Автор 1\nАвтор 2\nАвтор 3
```

**Параметры:**
- `[section_name]` — label для запуска аддона (должен существовать в .rpy файле)
- `name` — отображаемое название аддона
- `description` — описание аддона, поддерживает перенос строк с помощью `\n`
- `avatar` — путь к изображению аватара (необязательно)
- `authors` — список авторов (необязательно)

### Пример script.rpy для аддона

```renpy
label your_addon_label:
    scene bg black
    "Добро пожаловать в ваш аддон!"
    
    return
```

## Поиск и фильтрация

### Функции поиска
- Поиск по **названию** аддона
- Поиск по **описанию** аддона
- Поиск по **авторам** аддона

Поддерживается сортировка списка аддонов по алфавиту.

## Конфигурация (TBAddonManager.ini)

Конфигурация автоматически создаётся при первом запуске лаунчера менеджера модов. Конфигурацию можно редактировать как в самом лаунчере, так и сам конфигурационный файл.
```ini
[gameconfig]
enabled = 1      # Включить/выключить менеджер (0/1)
developer = 0    # Режим разработчика (0/1)
console = 0      # Консоль Shift+O (0/1)
```

### Режим разработчика
При `developer = 1` и `console = 1`:
- Автоматически очищается консоль при запуске
- Доступна консоль разработчика (Shift+O)
- Отображаются отладочные сообщения

## 📝 Известные ограничения
Вот тут надо чё-то придумать или вообще убрать.
1. **Аватары** — максимальный размер 256x256px для оптимальной производительности
2. **Описание** — рекомендуется не более 500 символов для читаемости
3. **Название** — адаптивный размер шрифта для названий до 60 символов
4. **Поиск** — работает только с текстом, поиск по изображениям не поддерживается